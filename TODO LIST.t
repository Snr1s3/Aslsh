mv — Move or rename files and directories
cp — Copy files and directories
rm — Remove files and directories
mkdir — Create new directories
rmdir — Remove empty directories
grep — Search for patterns in files
head — Show the first lines of a file
tail — Show the last lines of a file
wc — Count lines, words, and bytes in files
date — Display or set the system date and time
whoami — Show the current user
chmod — Change file permissions
chown — Change file owner and group
ps — Show running processes
kill — Send signals to processes
which — Show the full path of shell commands
history -c — Clear the command history
uptime — Show how long the system has been running
env — Show environment variables
export VAR=val — Set environment variables (advanced)
source file — Run commands from a file (improve existing)